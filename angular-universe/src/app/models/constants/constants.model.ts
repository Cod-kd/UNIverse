export interface Role {
    id: number;
    name: string;
}

export interface ContactType {
    id: number;
    name: string;
    domain: string;
    protocol: string;
}

export interface Category {
    id: number;
    name: string;
}